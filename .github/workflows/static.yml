# Simple workflow for deploying static content to GitHub Pages
name: Deploy DataJourney Stats

on:
  # Runs on pushes targeting the default branch
  # push:
  #   branches: ["github-repo-stats"]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    environment:
        name: github-pages
        url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout github-repo-stats
        uses: actions/checkout@v3
        with:
          ref: github-repo-stats
      - name: Change directory and print it
        run: |
          cd sayantikabanik/DataJourney/latest-report
          pwd
      - name: List files in the current directory
        run: ls -al
        working-directory: sayantikabanik/DataJourney/latest-report
      - name: Rename the file to index.html
        run: |
         mv report.html index.html
        working-directory: sayantikabanik/DataJourney/latest-report
      - name: Upload HTML files to GitHub Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: sayantikabanik/DataJourney/latest-report/

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: sayantikabanik/DataJourney/latest-report/
      # - name: Deploy to GitHub Pages
      #   uses: peaceiris/actions-gh-pages@v3
      #   with:
      #     github_token: ${{ secrets.GITHUB_TOKEN }}
      #     publish_dir: sayantikabanik/DataJourney/latest-report


# # Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
# permissions:
#   contents: write
#   pages: write
#   id-token: write

# # Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# # However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
# concurrency:
#   group: "pages"
#   cancel-in-progress: false

# jobs:
#   # Single deploy job since we're just deploying
#   deploy:
#     environment:
#       name: github-pages
#       url: ${{ steps.deployment.outputs.page_url }}
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout to resident stat branch
#         uses: actions/checkout@v4
#         with:
#           ref: github-repo-stats
#       # - name: Deploy to GitHub Pages
#       #   uses: peaceiris/actions-gh-pages@v4
#       #   with:
#       #     github_token: ${{ secrets.GITHUB_TOKEN }}
#       #     publish_dir: ./latest-report/report.html
#       - name: Setup Pages
#         uses: actions/configure-pages@v5
#       - name: Upload artifact
#         uses: actions/upload-pages-artifact@v3
#         with:
#           # Upload only the html file
#           path: './latest-report/report.html'
#       - name: Deploy to GitHub Pages
#         id: deployment
#         uses: actions/deploy-pages@v4
